stop_on_failure: true

suites:
    core_suite:
        namespace: Domain\Core
        psr4_prefix: Domain\Core
        spec_path: src/Domain/Core
        src_path: src/Domain/Core
        spec_prefix: Spec

    task_infrastructure_suite:
        namespace: TaskBundle
        psr4_prefix: TaskBundle
        spec_path: src/TaskBundle
        src_path: src/TaskBundle
        spec_prefix: Spec

    task_suite:
        namespace: Domain\Task
        psr4_prefix: Domain\Task
        spec_path: src/Domain/Task
        src_path: src/Domain/Task
        spec_prefix: Spec

    user_infrastructure_suite:
        namespace: UserBundle
        psr4_prefix: UserBundle
        spec_path: src/UserBundle
        src_path: src/UserBundle
        spec_prefix: Spec

    user_suite:
        namespace: Domain\User
        psr4_prefix: Domain\User
        spec_path: src/Domain/User
        src_path: src/Domain/User
        spec_prefix: Spec
